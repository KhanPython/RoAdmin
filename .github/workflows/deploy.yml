name: Deploy RoAdmin Bot

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Google Cloud VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # === CONFIGURATION: PASTE YOUR PATHS HERE ===
            GIT_PATH="/usr/bin/git"  # Replace with output of 'which git'
            NPM_PATH="/usr/bin/npm" # Replace with output of 'which npm'
            PM2_PATH="/usr/local/bin/pm2" # Replace with output of 'which pm2'
            # ============================================

            # 1. Go to folder
            cd ~/RoAdmin

            # 2. Force the VM to match GitHub exactly
            $GIT_PATH fetch --all
            $GIT_PATH reset --hard origin/master
            $GIT_PATH clean -fd  # This removes any untracked files/folders

            # 3. Update .env
            echo 'DISCORD_TOKEN="${{ secrets.DISCORD_TOKEN }}"' > .env
            echo 'ENCRYPTION_KEY="${{ secrets.ENCRYPTION_KEY }}"' >> .env
            echo 'NODE_ENV="production"' >> .env
            echo 'LOG_LEVEL="info"' >> .env

            # 4. Install dependencies (using absolute npm path)
            $NPM_PATH install --production

            # 5. Stop any existing instance, then start fresh from the ROOT directory
            $PM2_PATH delete RoAdmin 2>/dev/null || true
            $PM2_PATH start src/index.js --name "RoAdmin" --cwd "/home/orazimbetov_jalaladdin1/RoAdmin"
            $PM2_PATH save

            echo "Deployed successfully using absolute paths."
